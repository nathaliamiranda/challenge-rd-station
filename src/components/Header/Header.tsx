import Button from './Button';
import Logo from './Logo';
import Menu from './Menu';

function Header() {
  return (
    <header>
      <Logo/>
      <Menu/>
      <Button/>
      <div></div>
    </header>
  )
}

export default Header;